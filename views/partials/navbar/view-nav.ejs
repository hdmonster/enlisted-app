<div class="navbar">
    <div class="sec_wrapper" onclick="back()">
        <i data-feather="arrow-left"></i>
    </div>
    <div class="title_wrapper">
        <span><%- nav_title %></span>
    </div>
    <div class="primary_wrapper">
        <% if (title.includes('List')) { %>
            <a href="javascript:shareList('<%= list.title %>')">
                <i data-feather="share-2"></i>
            </a>
        <% } %>
    </div>
</div>